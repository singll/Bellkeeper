# Bellkeeper

SilkSpool 的知识治理中台 + LLM 代理网关 + Matrix 控制平面。做 n8n 做不好的有状态工作：长连接、持久化队列、分类去重、检索、文件治理。

## Language

**知识管线**:
从 URL 入库到 Meilisearch 可检索的全链路，含爬取、提取、分类、入库、索引
_Avoid_: 知识流程、入库管线

**入库**:
URL/内容经去重、提取、分类后落盘为 Markdown 并写入 DB 元数据的动作
_Avoid_: 导入、导入库

**知识层**:
raw / archive / vault 三级存储分层，决定文件是否进 Meili 索引和 Obsidian 同步
_Avoid_: 存储层、文件层

**策展**:
PKB 的打分→分流→重构→摘要批处理流程
_Avoid_: 整理、治理

**知识骨架**:
LLM 自上而下生成、人只调大方向的领域目标概念树；以 vault markdown（`_index.md` 根 + `topics/` MOC）形式存在，由 digest 独占增量维护（护栏6 快照+原子替换），人不手改、只经 Obsidian 同步查看并通过前端调方向。每个叶子标记「已填(链到卡片)」或「缺口」，缺口驱动定向填充；digest 不再自行猜测树结构
_Avoid_: 知识树、大纲、目录、教纲

**归位**:
把一张原子卡挂到知识骨架的某个节点上的动作；每轮 digest 用最新骨架对待归位卡片重新匹配
_Avoid_: 挂载、归类、分类

**待归位卡片**:
已入 vault 但还没挂到任何骨架节点的原子卡；是一种挂载状态而非新知识层，也是涌现回流（提议给骨架加节点）的原料
_Avoid_: 孤儿卡（孤儿=0 横向链接，与是否挂骨架无关，是另一概念）

**待归位区**:
pkb-curate 每轮重新渲染的 MOC 视图，列出待归位卡片；分领域级（`vault/<领域>/_待归位.md`）与全局级（`vault/_待归位.md`）两层，非新目录树而是过滤投影
_Avoid_: 收件箱、暂存区

**知识库**:
PKB 的稳定体系子系统，按知识骨架树组织、激进去重、价值衰减慢；回答「这个领域的知识全貌是什么」
_Avoid_: 体系库、PKB（PKB 是含知识库+资讯库的总称）

**资讯库**:
PKB 的时效流子系统，按「领域+每日」组织（`资讯/<领域>/<YYYY-MM-DD>.md`，一天一文件，与现有 DailyReportService 日报联动）、不进骨架、宽松去重、价值衰减快、只追加不删；复用现有 RSS 爬取与 brief/日报机制，资讯条目只活在每日存档里不存独立卡，回答「今天这个领域发生了什么」
_Avoid_: 新闻库、情报库

**晋升**:
LLM 把资讯库流中识别出的耐久知识点（原理/方法/模式，非事件）沉淀进知识库骨架的动作，由 score 的 durability+novelty 维度把闸
_Avoid_: 沉淀、转化、归档

**核实**:
抓取卡片自报引用源 + 一次 LLM 校验该源是否支撑卡片内容的动作；通过记 verified 高置信，抓不到或不支撑则降级
_Avoid_: 验证、查证

**出处**:
卡片内容的来源方式与可信级别，写入 frontmatter：crawled/verified（背靠真实核实源）、unverified（有自报引用未核实）、llm-only（无任何引用）
_Avoid_: 来源、provenance、引用

**置信度**:
由出处+核实结果派生的卡片可信等级（高/中/低）；低置信卡进 audit 计数作为网健康度指标
_Avoid_: 可信度、可靠性

**缺口填充**:
遍历知识骨架的缺口节点，LLM 起草卡片+提议权威源 → 用 Extract 原语抓取（先读域名冷却表让路）→ V2 核实 → 归位的填充循环；区别于 RSS 涌现入库，是自上而下定向补结构
_Avoid_: 补全、补洞、定向爬取

**影响半径**:
一次骨架结构变更（删/合并/重构节点）所触及的已挂卡片数；≤ 可配阈值视为小动作、快照后自动应用，> 阈值视为大动作、推 Matrix 批准
_Avoid_: 影响范围、波及面

**调方向前端**:
Bellkeeper Web 前端的职责边界（DC 决策的正向表述）。原则一句话：Web 管「机制与观测」，Obsidian/文件系统管「产物与浏览」。允许三类——①只读聚合：散在 vault、Obsidian 看不出的全局统计/状态/健康度/结构投影（总览）；②操控生成机制：批准骨架提议、设大方向、剪节点，乃至直接编辑「卡片如何被生成」的规则与配置（scope/阈值/生成·填充策略），产物随后由机器据此重新生成；③对话问答。判据看作用对象：作用于「机制/规则/方向」或「只读观测产物」→ 允许（哪怕越界到直接改规则）；作用于「产物本身」（卡片正文、文件、目录树的增删改）→ 否决，归 Obsidian/文件系统。**唯一例外**：资讯库每日存档（时效报告）允许 Web 只读渲染 + 时间线浏览（ADR-0006）。不违反 W1（产物机器独占写）
_Avoid_: 管理后台、知识库浏览器、Web 卡片编辑/CRUD、目录树浏览

**对话问答**:
Matrix 对话房间内直接说话触发的 agentic LLM 问答：模型可自主调用知识库检索等工具，命中知识卡片时在回答里附只读引用；多轮上下文以房间为单位保持（房间即会话），模型用房间默认模型组、可经命令切换。区别于 `!kb` 强制纯知识库检索（RAG-only、不走通用对话）
_Avoid_: 知识库问答、RAG 问答、!问

**对话房间**:
承载「对话问答」的 Matrix 房间类型（room_type=chat）；其非命令消息直接触发对话 agent，命令/通知/管理房间不受影响
_Avoid_: 聊天室、agent 房间

**渠道**:
LLM API 的一个具体提供商端点（如 DashScope、DeepSeek），含凭证和模型列表
_Avoid_: provider、供应商

**模型组**:
虚拟模型名（如 pool-chat-free），按策略解析到具体渠道的模型
_Avoid_: 虚拟模型、模型池

**会话粘性**:
同一 ConversationID 的请求绑定到同一渠道以利用 prompt cache
_Avoid_: 粘性路由、会话绑定

**限流学习**:
基于 429 响应自适应调整渠道 RPM 的机制
_Avoid_: 自适应限流、限流适配

**命令路由**:
Matrix 消息的 `!command` 前缀解析与权限检查后的分发
_Avoid_: 命令分发、命令处理

**通知网关**:
NATS 队列 → Worker → Matrix 消息投递的管道
_Avoid_: 通知管道、消息网关

**工作流漂移**:
本地 `internal/n8n_workflows/*.json` 与 n8n 运行态的工作流定义不一致
_Avoid_: 工作流不同步、定义偏差

**心跳**:
Go 后台服务定期写 activity_logs 的存活信号（module=heartbeat），超过 15 分钟无心跳视为异常
_Avoid_: 存活检测、健康探针

**错误传播**:
n8n 节点失败后错误是否向上传播到工作流整体 error 状态；加 onError 则吞错误（不传播），不加则传播（触发 B02）
_Avoid_: 错误冒泡、失败透传

**分桶**:
按域名 24h 通过率将域名归入高速(≥30%)、中速(10-30%)、低速(<10%) 三个虚拟队列，Worker 按优先级降级取 job
_Avoid_: 队列分片、域名分区

**冷却**:
域名连续失败后暂停处理该域名 job 的临时状态，期间桶内其他域名正常处理（让路），冷却到期后用新策略重试
_Avoid_: 冻结、暂停、黑名单

**冷却让路**:
冷却中域名的 job 在 Dequeue SQL 层被排除，Worker 直接取到可处理的域名 job，不空转
_Avoid_: 跳过、延迟

**学习分析**:
域名进入冷却时立即调用 LLM 分析失败原因，输出完整的请求覆盖规则（HTTP headers、timeout、提取策略、Firecrawl 参数等），冷却到期后用新方案重试
_Avoid_: 规则生成、自适应调整

**请求覆盖规则**:
LLM 学习分析输出的完整请求调整方案，覆盖默认的 HTTP header、超时、提取策略优先级、Firecrawl waitFor/actions 等全部参数
_Avoid_: 提取规则、发包规则

**爬取失败档案**:
独立于 job 队列的持久化实体，记录多次失败 URL 的累计信息，支持查询和恢复入队
_Avoid_: 死信队列、失败记录
